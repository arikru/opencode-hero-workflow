---
description: Exercise a feature or codebase as a real user — happy-path → adversarial → source read → HTML report
argument-hint: [seed]
---

Load the `hero-dogfood` skill and begin a dogfooding session.

Pass the user's argument (if any) as the seed. If no seed was given, the skill will discover the scope automatically and announce it before proceeding.
